jQuery(document).ready(function($){

    $(".aime .work-info").owlCarousel({
 
      navigation : false, // Show next and prev buttons
      pagination : true,
      slideSpeed : 300,
      // autoPlay: 9000,
      paginationSpeed : 400,
      singleItem:true
 
    });

});