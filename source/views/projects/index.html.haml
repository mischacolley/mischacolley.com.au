---
title: Projects
---

%section

  %h1 Projects
  %p.section-desc a place for side projects

  .articles
    - data.projects.projects.each_with_index do |projects, index|
      %article.projects
        = image_tag "projects/#{projects.image}", resize_to: '960x378', class: 'thumbnail'
        .article-inner
          %h3
            = link_to "#{projects.title}", "#{projects.permalink}"
          %p=projects.summary