---
title: Blog | Mischa Colley
pageable: true
per_page: 10
---

- if paginate && num_pages > 1
  %p
    Page #{page_number} of #{num_pages}
  - if prev_page
    %p= link_to 'Previous page', prev_page

- page_articles.each_with_index do |article, i|
  %article
    .post-heading
      .category
        - category = article.data.category
        = link_to category, category_path(category)
      %h1
        = link_to article.title, article.url 
      .tags 
        - article.tags[0...1].each do |tag, articles|
          = link_to "#{tag}", tag_path(tag)

    %img{:src => article.url+"featured_image.jpg"}
    .article-inner.article
      %span= article.date.strftime('%e %b %Y')
      .summary
        %p= Nokogiri::HTML(article.summary).text
        = link_to "Read more", "#{article.url}", :class => 'read-more'
- if paginate
  - if next_page
    %p= link_to 'Next page', next_page